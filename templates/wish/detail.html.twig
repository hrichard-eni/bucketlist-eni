{% extends 'base.html.twig' %}

{% block body %}

{% endblock %}

{% block main %}
    <div class="container">
        <div class="row">
<h1>Détails</h1>
    <p><strong>Titre : </strong>{{ wishSelect.title }}</p>
    <p><strong>Description : </strong>{{ wishSelect.description }}</p>
    <p><strong>Auteur : </strong>{{ wishSelect.author }}</p>
    <p><strong>Catégorie : </strong>{{ wishSelect.category.name }}</p>
    <p><strong>Date de création : </strong>{{ wishSelect.dateCreated|date("m/d/Y H:i:s") }}</p>
        </div>
    </div>
{% endblock %}

